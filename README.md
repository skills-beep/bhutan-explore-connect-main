# Bhutan Explore Connect

Bhutan Explore Connect is a travel discovery website for exploring Bhutanese destinations, curated travel packages, local hosts, and travel companions.

## Local development

```bash
npm install
npm run dev
```

The app runs on the Vite development server. Backend services can be started separately with `npm run dev:backend`.
